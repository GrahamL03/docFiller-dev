// optionPasswordField.ts
// In the MVP, no API key fields are needed.
// Keep an initializer so imports don’t break.

export function initializeOptionPasswordField() {
  // Nothing to initialize — API key fields removed
  return;
}
